#!/usr/local/bin/mpiguash

println("Starting a new process...");

rank = MPI_Comm_rank();
size = MPI_Comm_size();

println("Process #" + rank + " running!");
println("Nunber of processes: " + size + ".");

MPI_Finalize();
